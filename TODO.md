# TODO

- When screen capture failure (which leading to the L progress indicator), the fairy agent should be skipped, since nothing useful information can be provided.
  - It seems when the screen is locked the main monitor still could be captured, so actually any monitor error should leading to a skipping, not the current all monitor errors condition.
